Trie structure that keeps a dictionary of words.

Supports
 - adding new words
 - look up queries
    - exact queries                 IMPLEMENTED
    - wildcards in the pattern      PARTIALLY IMPLEMENTED, need to fix some bugs

[2013-07-18] - Added test cases for exact matching and wildcards in the pattern (k=5).
             - JUnit test cases (TestExactMatching, TestWildcardsInThePattern)

TODO;
    - Add support for wildcards in the text.
